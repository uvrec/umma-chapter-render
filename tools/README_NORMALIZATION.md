# Інструкція по нормалізації існуючих текстів

## Огляд

Цей пакет містить інструменти для нормалізації існуючих текстів Чайтанья-чарітамріти в базі даних.

**Що робить нормалізація:**

1. **Українські тексти** (synonyms_ua, translation_ua, commentary_ua):
   - `н'` → `нь` (апостроф після "н" замінюється на м'який знак)
   - `Санн'ясі` → `Санньясі` (всі відмінки)
   - `джджг` → `джджх`
   - `джг` → `джх`

2. **Англійські Synonyms** (synonyms_en):
   - Видалення повторюваних слів зі збереженням порядку
   - Приклад: `"the the word word"` → `"the word"`

## Файли

- **`supabase/migrations/20251103224604_add_text_normalization_functions.sql`** - Міграція для Supabase (створює функції в базі)
- **`tools/normalize_existing_texts.sql`** - Standalone SQL скрипт для локального виконання
- **`tools/README_NORMALIZATION.md`** - Цей файл з інструкціями

## Варіант 1: Через Supabase Dashboard (Рекомендовано)

### Крок 1: Створити резервну копію

⚠️ **ОБОВ'ЯЗКОВО** створіть резервну копію перед виконанням!

Через Supabase Dashboard:
1. Перейдіть до **Database** → **Backups**
2. Натисніть **"Create backup"**
3. Зачекайте завершення

### Крок 2: Застосувати міграцію

1. Перейдіть до **SQL Editor** у Supabase Dashboard
2. Натисніть **"New query"**
3. Скопіюйте вміст файлу `supabase/migrations/20251103224604_add_text_normalization_functions.sql`
4. Вставте у SQL Editor
5. Натисніть **"Run"** або `Cmd/Ctrl + Enter`

Це створить дві функції:
- `normalize_ukrainian_cc_texts()`
- `remove_duplicate_words_in_synonyms()`

### Крок 3: Використовувати через UI

1. Задеплойте frontend зміни через Lovable (щоб з'явилась сторінка `/admin/normalize`)
2. Перейдіть на `/admin/normalize`
3. Натисніть кнопку **"Нормалізувати українські тексти"**
4. Дочекайтеся завершення (з'явиться повідомлення про успіх)
5. За бажанням: **"Очистити англійські synonyms від дублів"**

### Крок 4: Перевірити результати

Виконайте в SQL Editor:

```sql
-- Перевірка: скільки залишилось н' (має бути 0 або дуже мало)
SELECT COUNT(*) AS verses_with_apostrophe_n
FROM verses v
JOIN chapters ch ON v.chapter_id = ch.id
JOIN books b ON ch.book_id = b.id
WHERE b.slug LIKE 'cc-%'
  AND (v.synonyms_ua LIKE '%н''%'
       OR v.translation_ua LIKE '%н''%'
       OR v.commentary_ua LIKE '%н''%');

-- Приклади нормалізованих віршів
SELECT
  v.verse_number,
  LEFT(v.synonyms_ua, 100) AS synonyms,
  LEFT(v.translation_ua, 100) AS translation
FROM verses v
JOIN chapters ch ON v.chapter_id = ch.id
JOIN books b ON ch.book_id = b.id
WHERE b.slug LIKE 'cc-%'
LIMIT 10;
```

## Варіант 2: Локально через psql

### Передумови

- PostgreSQL клієнт (`psql`) встановлений
- Доступ до бази даних (host, port, user, password, database)

### Крок 1: Налаштувати з'єднання

Створіть файл `.pgpass` (для безпечного зберігання паролю):

```bash
# ~/.pgpass (chmod 600)
your-host:5432:vedavoice:your_user:your_password
```

Або експортуйте змінні оточення:

```bash
export PGHOST=your-host
export PGPORT=5432
export PGDATABASE=vedavoice
export PGUSER=your_user
export PGPASSWORD=your_password
```

### Крок 2: Створити резервну копію

```bash
# Резервна копія всієї таблиці verses
pg_dump -t verses > backup_verses_$(date +%Y%m%d_%H%M%S).sql

# Або тільки дані (без схеми)
pg_dump -t verses --data-only > backup_verses_data_$(date +%Y%m%d_%H%M%S).sql

# Або тільки CC вірші (потребує кастомного запиту)
psql -c "COPY (
  SELECT v.* FROM verses v
  JOIN chapters ch ON v.chapter_id = ch.id
  JOIN books b ON ch.book_id = b.id
  WHERE b.slug LIKE 'cc-%'
) TO STDOUT" > backup_cc_verses_$(date +%Y%m%d_%H%M%S).csv
```

### Крок 3: Виконати нормалізацію

```bash
# Виконати SQL скрипт
psql -f tools/normalize_existing_texts.sql

# Або з логуванням
psql -f tools/normalize_existing_texts.sql > normalization_log_$(date +%Y%m%d_%H%M%S).log 2>&1
```

Скрипт виведе:
- Приклади віршів ДО нормалізації
- Список змінених віршів з деталями
- Статистику ПІСЛЯ нормалізації

### Крок 4: Перевірити результати

Перегляньте лог файл або виконайте перевірочні запити:

```bash
# Скільки віршів було оновлено
psql -c "SELECT COUNT(*) FROM verses WHERE updated_at > NOW() - INTERVAL '10 minutes';"

# Приклади нормалізованих текстів
psql -c "
SELECT verse_number, LEFT(synonyms_ua, 100)
FROM verses v
JOIN chapters ch ON v.chapter_id = ch.id
JOIN books b ON ch.book_id = b.id
WHERE b.slug = 'cc-adi'
LIMIT 5;
"
```

## Варіант 3: Через Supabase CLI

### Передумови

```bash
# Встановити Supabase CLI
npm install -g supabase

# Залогінитись
supabase login
```

### Виконання

```bash
# Застосувати міграцію до remote проекту
supabase db push

# Або виконати SQL напряму
supabase db execute -f tools/normalize_existing_texts.sql
```

## Відкат змін (якщо щось пішло не так)

### Через Supabase Dashboard

1. Database → Backups
2. Виберіть backup створений перед нормалізацією
3. Натисніть **"Restore"**

### Через psql

```bash
# Відновити з backup файлу
psql -f backup_verses_YYYYMMDD_HHMMSS.sql

# Або якщо це був COPY backup
psql -c "TRUNCATE verses CASCADE;"
psql -c "\COPY verses FROM 'backup_cc_verses_YYYYMMDD_HHMMSS.csv'"
```

## Часті питання (FAQ)

### Q: Скільки часу займе нормалізація?

**A:** Залежить від кількості віршів:
- ~1000 віршів: 5-10 секунд
- ~10000 віршів: 30-60 секунд
- ~50000+ віршів: 2-5 хвилин

### Q: Чи безпечно запускати нормалізацію кілька разів?

**A:** Так! Функції ідемпотентні - якщо текст вже нормалізований, вони нічого не зроблять. Повторне виконання не пошкодить дані.

### Q: Чи можна нормалізувати тільки одну книгу?

**A:** Так, змініть фільтр у SQL функції:

```sql
-- Замість WHERE b.slug LIKE 'cc-%'
WHERE b.slug = 'cc-adi'  -- Тільки Аді-ліла
```

### Q: Що робити якщо виникла помилка "function does not exist"?

**A:** Спочатку застосуйте міграцію (Варіант 1, Крок 2) щоб створити функції в базі.

### Q: Як побачити які саме зміни будуть застосовані БЕЗ оновлення даних?

**A:** Виконайте тестовий запит:

```sql
-- Приклад віршів що будуть змінені
SELECT
  v.verse_number,
  v.synonyms_ua AS before,
  regexp_replace(
    regexp_replace(v.synonyms_ua, 'н''', 'нь', 'g'),
    'Н''', 'Нь', 'g'
  ) AS after
FROM verses v
JOIN chapters ch ON v.chapter_id = ch.id
JOIN books b ON ch.book_id = b.id
WHERE b.slug LIKE 'cc-%'
  AND v.synonyms_ua LIKE '%н''%'
LIMIT 10;
```

## Контакти та підтримка

При виникненні проблем:
1. Перевірте логи нормалізації
2. Виконайте перевірочні запити
3. Створіть issue у GitHub репозиторії
4. Опишіть: яким методом виконували, які помилки, приклади неправильних результатів

## Технічні деталі

### Правила нормалізації (докладно)

**1. Апостроф після "н" → м'який знак**

Правило: В українській мові після "н" використовується м'який знак, а не апостроф.

```
НЕПРАВИЛЬНО ✗        ПРАВИЛЬНО ✓
сан'н'ясі            санньясі
н'ячарі              ньячарі
ачар'н'я             ачарнья
```

Regex: `regexp_replace(text, 'н''', 'нь', 'g')`

**2. Санн'ясі → Санньясі (всі відмінки)**

Спеціальний випадок для слова "санньясі" та його форм.

```
санн'ясі → санньясі
санн'яса → санньяса
санн'ясою → санньясою
Санн'ясі → Санньясі
```

**3. джджг → джджх**

Виправлення неправильної транслітерації подвійного придихового.

```
проджджгіта → проджджхіта
```

**4. джг → джх**

Виправлення одиночного придихового (але НЕ в "джджг" яке вже оброблено).

```
праджг → праджх
маджга → маджха
```

Regex з negative lookahead: `regexp_replace(text, 'джг(?!джх)', 'джх', 'g')`

### Видалення дублікатів у Synonyms

Алгоритм:
1. Розбити текст на слова по пробілах: `string_to_array(text, ' ')`
2. Пройтись по словах, додаючи тільки унікальні до результату
3. Зберегти порядок першого входження
4. З'єднати назад: `array_to_string(unique_words, ' ')`

Приклад:
```
До:    "śrī śrī caitanya caitanya mahāprabhu mahāprabhu"
Після: "śrī caitanya mahāprabhu"
```

### Оптимізація продуктивності

Функції використовують:
- Batch оновлення в транзакції
- Умовне оновлення (тільки якщо щось змінилось)
- `updated_at` автоматично оновлюється
- RAISE NOTICE для показу прогресу

## Автори

Created by Claude for VedaVOICE project (2025)

---

**Бажаю успішної нормалізації! 🙏**
