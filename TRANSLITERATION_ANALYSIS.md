# 🔍 Аналіз проблем транслітерації та подвійних полів

## 📊 Знайдені проблеми

### 1️⃣ "атга" замість "атха" ❌

**Проблема**: Користувач повідомив що в БД є "атга" замість правильного "атха"

**Правило**: `th` → `тх` (НІКОЛИ не "тг"!)

**Перевірені конвертери** (всі правильні ✅):
- `src/utils/dualSourceParser.ts:62` → `'th': 'тх'`
- `src/utils/textNormalizer.ts:230` → `'th': 'тх'`
- `src/utils/text/transliteration.ts:45` → `'th': 'тх'`

**Де шукати джерело**:
1. Запустити `find_atga_problem.sql` в Supabase SQL Editor
2. Перевірити які книги/вірші містять "атга"
3. Можливі джерела:
   - Старий імпорт з неправильним конвертером
   - Пряме використання Sanscript без наших правил
   - Ручне введення даних

---

### 2️⃣ Одинарні поля замість подвійних ❌

**Проблема**: В БД існують застарілі одинарні поля:
- `transliteration` (замість `transliteration_ua` + `transliteration_en`)
- `synonyms` (замість `synonyms_ua` + `synonyms_en`)
- `sanskrit` (це ОК - Devanagari однаковий для обох мов ✅)

**Знайдено використання в коді** (15+ файлів):
```
src/utils/import/importer.ts
src/utils/import/splitters.ts
src/pages/admin/VedabaseImportV3.tsx
src/pages/KnowledgeCompiler.tsx
... і багато інших
```

**Що треба**:
1. Запустити `diagnose_single_fields.sql` щоб побачити масштаб проблеми
2. Створити план міграції даних
3. Оновити всі TS типи
4. Оновити всі SQL запити

---

## 🔧 Створені діагностичні інструменти

### `find_atga_problem.sql`
Знаходить вірші з неправильною транслітерацією "атга":
- Перевіряє `transliteration_ua` та `transliteration`
- Шукає всі варіанти "тг" перед голосними
- Показує статистику по книгах

### `diagnose_single_fields.sql`
Аналізує використання одинарних vs подвійних полів:
- Схема таблиці `verses`
- Статистика заповнення полів
- Книги що використовують стару схему
- Конфлікти (обидва поля заповнені)

---

## 📋 План дій

### Етап 1: Діагностика (ЗАРАЗ)
1. ✅ Створено SQL скрипти діагностики
2. ⏳ Запустити в Supabase SQL Editor
3. ⏳ Проаналізувати результати

### Етап 2: Міграція даних
1. Створити backup БД
2. Скопіювати дані з одинарних → подвійні поля
3. Для `transliteration`:
   - Якщо є українська → `transliteration_ua`
   - Якщо є IAST → `transliteration_en`
   - Якщо тільки IAST → конвертувати в `transliteration_ua`
4. Для `synonyms`:
   - Розділити на UA/EN частини
   - Або залишити як `synonyms_en` якщо тільки англійська

### Етап 3: Оновлення коду
1. Оновити TypeScript типи
2. Замінити всі використання одинарних полів
3. Оновити імпортери
4. Оновити UI компоненти

### Етап 4: Видалення старих полів (опціонально)
1. Після повної міграції
2. Видалити колонки `transliteration` та `synonyms`

---

## 🎯 Правильний підхід для кожної книги

### Caitanya-caritamrta (CC) ✅
- **Джерело UA**: Gitabase (`gitabase.com/ukr/CC/`)
- **Джерело EN**: Vedabase (`vedabase.io/en/library/cc/`)
- **Парсер**: `dualSourceParser.ts` → `mergeVedabaseAndGitabase()`
- **Транслітерація UA**: Конвертується з IAST Vedabase
- **Synonyms UA**: Терміни з IAST + переклади з Gitabase

### Śrīmad-Bhāgavatam (SB) ⚠️
- **Джерело UA**: EPUB файли (`UK_SB_*_epub_r*.epub`)
- **Джерело EN**: Vedabase (`vedabase.io/en/library/sb/`)
- **Парсер**: ❌ ВИДАЛЕНИЙ (був небезпечний)
- **Потрібно**: Створити НОВИЙ безпечний EPUB парсер
- **Транслітерація UA**: З EPUB або конвертована з EN
- **Synonyms UA**: Генерувати з EN термінів + fallback на EN переклади

### Bhagavad-gita (BG)
- **Джерело UA**: Вручну або EPUB
- **Джерело EN**: Vedabase
- **Gitabase**: ❌ НЕМАЄ UA версії

### Nectar of Instruction (NoI) ✅
- **Джерело UA**: Gitabase (`gitabase.com/ukr/NoI/`)
- **Джерело EN**: Vedabase
- **Парсер**: `noiParser.ts`

---

## ⚠️ КРИТИЧНО: Gitabase

**НЕ ЧІПАТИ механізм Gitabase!**

- Gitabase потрібен для Caitanya-caritamrta (вже імпортована ✅)
- Gitabase має ТІЛЬКИ CC та NoI
- Для інших книг (SB, BG, ISO...) Gitabase НЕ використовується

**Конфігурація** (`src/utils/Vedabase-books.ts`):
```typescript
{
  slug: 'sb',
  hasGitabaseUA: false, // ✅ Правильно
  gitabase_available: false,
},
{
  slug: 'cc',
  hasGitabaseUA: true, // ✅ Правильно
  gitabase_available: true,
}
```

---

## 📝 Примітки

1. **Для кожного імпорту свій підхід** - користувач наголосив на цьому
2. **tools/TRANSLITERATION_RULES.md** - містить правила СТИЛЮ, а не конвертації IAST
3. **tools/translit_normalizer.py** - тільки очищає текст, НЕ конвертує IAST
4. **Правильний конвертер**: `src/utils/textNormalizer.ts` → `convertIASTtoUkrainian()`

---

**Автор**: Claude Code
**Дата**: 2025-11-17
**Версія**: 1.0
