# VEDAVOICE.ORG — Інструкції для AI

> **КРИТИЧНО**: Цей файл ОБОВ'ЯЗКОВИЙ для дотримання при БУДЬ-ЯКИХ змінах коду.
> Lovable, Claude Code, та інші AI-агенти ПОВИННІ дотримуватись цих правил.

## 🎨 ДИЗАЙН-СИСТЕМА

### Головне правило: МІНІМАЛІЗМ

Сайт — це **цифрова книга**. Дизайн має бути чистим, без візуального шуму.

### ❌ НЕ ВИКОРИСТОВУВАТИ:

1. **Card компоненти** — ЗАБОРОНЕНО (виняток: обкладинки книг у бібліотеці)
2. **border (рамки)** — ЗАБОРОНЕНО
3. **shadow (тіні)** — ЗАБОРОНЕНО
4. **rounded з background** — ЗАБОРОНЕНО (сірі блоки)
5. **border-t, border-b** — ЗАБОРОНЕНО (лінії-розділювачі)
6. **StatsBar, QuickActions з картками** — ЗАБОРОНЕНО

### ✅ ВИКОРИСТОВУВАТИ:

1. **bg-background** — прозорий фон
2. **Типографічна ієрархія** — розміри шрифтів, жирність для структури
3. **Простір (margin, padding)** — для візуального розділення
4. **Чистий текст** — як у книжці

---

## 🔝 STICKY HEADER — КРИТИЧНО!

Header ЗАВЖДИ має бути sticky. Ніколи не змінювати ці правила:

```css
.site-header {
  position: -webkit-sticky !important;
  position: sticky !important;
  top: 0 !important;
  z-index: 50 !important;
}
```

**НІКОЛИ НЕ ВИКОРИСТОВУВАТИ** `overflow: hidden` або `overflow-y: hidden` на:
- `html`
- `body`
- `#root`
- батьківських елементах header

Використовувати `overflow-x: clip` замість `overflow-x: hidden`.

---

## 📱 HEADER КОМПОНЕНТ

Header має клас `site-header` і такі стилі:

```tsx
<header className="site-header border-b border-border/50 bg-background/70 backdrop-blur-md">
```

**НІКОЛИ НЕ ЗМІНЮВАТИ** клас `site-header` на інший!

---

## 🏠 HOMEPAGE (NewHome.tsx)

### Структура:
1. **AudioBanner** — банер з рекомендованими лекціями
2. **HomeSearchBar** — пошук (простий, без діалогу)
3. **DailyQuoteBanner** — цитата дня

### Заборонено на Homepage:
- QuickActions з картками
- StatsBar
- Будь-які Card компоненти
- Складні анімації

---

## 🎵 AUDIO PLAYER

Desktop: **fullscreen layout** (не картка)
Mobile: **fullscreen layout**

Ніколи не використовувати Card для плеєра.

---

## 📖 ЧИТАЛКА (VedaReader)

### Шрифти:
- **Основний текст**: `Noto Serif` (--font-primary)
- **Деванагарі**: `Noto Sans Devanagari` (--font-devanagari)
- **Транслітерація**: `Noto Serif` (--font-translit)

### Розміри:
Керуються через `useReaderSettings` hook та CSS змінні.

---

## 🔧 ТЕХНІЧНІ ПРАВИЛА

### При редагуванні файлів:

1. **ЗАВЖДИ перевіряти index.css** — там є критичні стилі
2. **НЕ видаляти коментарі** з правилами дизайну
3. **НЕ змінювати .site-header** стилі без крайньої необхідності
4. **НЕ додавати overflow: hidden** на body/html/#root

### Git:
- Всі зміни мають бути atomic commits
- Описувати що змінено в commit message

---

## 🚨 КРИТИЧНІ ФАЙЛИ (НЕ ЧІПАТИ БЕЗ ПРИЧИНИ)

1. `src/index.css` — базові стилі, дизайн-система
2. `src/components/Header.tsx` — навігація
3. `src/styles/fonts.css` — шрифти
4. `tailwind.config.ts` — конфігурація Tailwind

---

## 📋 CHECKLIST ПЕРЕД COMMIT

- [ ] Header залишається sticky?
- [ ] Немає нових Card компонентів (окрім бібліотеки)?
- [ ] Немає нових border/shadow?
- [ ] overflow-x використовує clip, не hidden?
- [ ] Шрифти не змінені?

---

**Останнє оновлення**: 2026-01-05
**Автор**: Claude Code
